{
  "scripts": {
    "build": "tsc --noEmit && node build.js",
    "watch": "nodemon --ignore \"./pub/main.js\" --exec \"npm run build\" --ext ts,html,vert,frag"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "esbuild-plugin-glsl": "^1.0.5",
    "esbuild": "^0.13.8",
    "webgl-strict-types": "^1.0.5"
  }
}
